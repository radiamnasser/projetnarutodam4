{% extends "admin/base_admin.html.twig" %}

{% block body %}

	<div class="container py-5">
		<div class="row">
			<div class="col-12">
				{% include "shared/_messages_flash.html.twig" %}
			</div>
			<div class="col-12">
				<div class="card">
					<div class="card-header">
						<h3>Formulaire de modification d'episode</h3>
					</div>
					<div class="card-body">
						{{ form_start(form) }}

						{{ form_widget(form) }}

						<button class="btn btn-primary mt-3" type="submit">Modifier</button>

						{{ form_end(form)}}
					</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}
